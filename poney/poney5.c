/*
** poney5.c for poney in /home/vincen_o/rendu/Igraph/RT
**
** Made by arthur vincent
** Login   <vincen_o@epitech.net>
**
** Started on  Mon May 26 15:26:35 2014 arthur vincent
** Last update Tue May 27 17:29:52 2014 arthur vincent
*/

#include	"poney.h"

void		lign21(t_win *mlxwin, int x, int y)
{
  aff_line_h(&x, y, 26, mlxwin, BLUEC);
  aff_line_h(&x, y, 2, mlxwin, BLUEF);
  aff_line_h(&x, y, 8, mlxwin, BLUEC);
  aff_line_h(&x, y, 6, mlxwin, BLUEF);
  aff_line_h(&x, y, 3, mlxwin, BLUEC);
  aff_line_h(&x, y, 2, mlxwin, BLUEF);
  aff_line_h(&x, y, 2, mlxwin, BLUEC);
  aff_line_h(&x, y, 1, mlxwin, BLUEF);
  aff_line_h(&x, y, 3, mlxwin, BLACK);
  aff_line_h(&x, y, 4, mlxwin, BLUEF);
}

void		lign22(t_win *mlxwin, int x, int y)
{
  aff_line_h(&x, y, 3, mlxwin, VIOLET);
  aff_line_h(&x, y, 8, mlxwin, BLUEC);
  aff_line_h(&x, y, 8, mlxwin, VIOLET);
  aff_line_h(&x, y, 3, mlxwin, BLUEC);
  aff_line_h(&x, y, 4, mlxwin, BLUEF);
  aff_line_h(&x, y, 9, mlxwin, BLUEC);
  aff_line_h(&x, y, 3, mlxwin, BLUEF);
  aff_line_h(&x, y, 3, mlxwin, BLACK);
  aff_line_h(&x, y, 5, mlxwin, BLUEF);
  aff_line_h(&x, y, 3, mlxwin, BLUEC);
  aff_line_h(&x, y, 1, mlxwin, BLUEF);
}

void		lign23(t_win *mlxwin, int x, int y)
{
  aff_line_h(&x, y, 21, mlxwin, VIOLET);
  aff_line_h(&x, y, 1, mlxwin, BLUEF);
  aff_line_h(&x, y, 7, mlxwin, BLUEC);
  aff_line_h(&x, y, 2, mlxwin, BLUEF);
  aff_line_h(&x, y, 3, mlxwin, BLUEC);
  aff_line_h(&x, y, 2, mlxwin, BLUEF);
  aff_line_h(&x, y, 8, mlxwin, BLACK);
  aff_line_h(&x, y, 1, mlxwin, BLUEF);
  aff_line_h(&x, y, 4, mlxwin, BLUEC);
  aff_line_h(&x, y, 1, mlxwin, BLUEF);
}

void		lign24(t_win *mlxwin, int x, int y)
{
  aff_line_h(&x, y, 21, mlxwin, VIOLET);
  aff_line_h(&x, y, 1, mlxwin, BLUEF);
  aff_line_h(&x, y, 5, mlxwin, BLUEC);
  aff_line_h(&x, y, 2, mlxwin, BLUEF);
  aff_line_h(&x, y, 1, mlxwin, BLUEC);
  aff_line_h(&x, y, 4, mlxwin, BLUEF);
  aff_line_h(&x, y, 10, mlxwin, BLACK);
  aff_line_h(&x, y, 1, mlxwin, BLUEF);
  aff_line_h(&x, y, 4, mlxwin, BLUEC);
  aff_line_h(&x, y, 1, mlxwin, BLUEF);
}

void		lign25(t_win *mlxwin, int x, int y)
{
  aff_line_h(&x, y, 21, mlxwin, VIOLET);
  aff_line_h(&x, y, 1, mlxwin, BLUEF);
  aff_line_h(&x, y, 4, mlxwin, BLUEC);
  aff_line_h(&x, y, 1, mlxwin, BLUEF);
  aff_line_h(&x, y, 3, mlxwin, BLUEC);
  aff_line_h(&x, y, 1, mlxwin, BLUEF);
  aff_line_h(&x, y, 13, mlxwin, BLACK);
  aff_line_h(&x, y, 1, mlxwin, BLUEF);
  aff_line_h(&x, y, 4, mlxwin, BLUEC);
  aff_line_h(&x, y, 1, mlxwin, BLUEF);
}
